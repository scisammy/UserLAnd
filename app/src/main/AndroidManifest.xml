<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- adbndk and qemu-backend require minSdk 28; suppress the merge error since both
         backends only run on API 28+ and are gated by build config flags at runtime -->
    <uses-sdk tools:overrideLibrary="tech.userland.adbndk,tech.ula.library.qemu"/>

</manifest>
